{
  "name": "@eight/thermometer",
  "version": "0.0.0",
  "description": "",
  "main": "build/index.js",
  "types": "build/index.d.ts",
  "files": [
    "build"
  ],
  "directories": {
    "test": "test"
  },
  "scripts": {
    "clean": "rm -rf build",
    "lint": "tslint --force --format verbose \"src/**/*.ts\"",
    "build": "npm run clean && npm run lint && tsc",
    "build:tests": "rm -rf test/build && tsc -p test/",
    "test": "npm run build:tests && nyc ava --verbose test/build/test"
  },
  "author": "",
  "license": "ISC",
  "nyc": {
    "cache": false,
    "exclude": [
      "build/**",
      "test/build/test/**"
    ],
    "reporter": [
      "json",
      "text",
      "lcovonly"
    ],
    "produce-source-map": true,
    "all": true
  },
  "devDependencies": {
    "@types/yargs": "^13.0.0",
    "ava": "1.4.1",
    "nyc": "14.0.0",
    "tslint": "5.16.0",
    "typescript": "3.4.5"
  },
  "dependencies": {
    "@eight/practices": "^6.9.20",
    "@eight/promises": "^1.1.0",
    "yargs": "^13.2.2"
  }
}
